# 线程池项目

## 简介
线程池是一种用于管理一组工作线程的机制，可以有效地处理大量异步任务。本项目实现了一个简单的线程池类 `ThreadPool`，并提供了示例程序演示其使用方法。

## 功能特点
- **多线程管理**：管理一组工作线程，提高任务执行效率
- **任务队列**：使用队列存储待执行任务，确保线程间合理竞争
- **线程安全**：使用 `std::atomic` 管理线程池停止标志
- **异步任务**：通过 `std::future` 和 `std::packaged_task` 执行异步任务并获取结果

